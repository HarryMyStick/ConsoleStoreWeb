{% extends 'base.html.twig' %}

{% block title %}Orderdetail{% endblock %}

{% block body %}
    <h1>Orderdetail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ orderdetail.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ orderdetail.Price }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ orderdetail.Quantity }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ orderdetail.Total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_orderdetail_index') }}">back to list</a>

    <a href="{{ path('app_orderdetail_edit', {'id': orderdetail.id}) }}">edit</a>

    {{ include('orderdetail/_delete_form.html.twig') }}
{% endblock %}
